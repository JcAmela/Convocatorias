/**
 * Stub del `import "jsr:@supabase/functions-js/edge-runtime.d.ts"` de la
 * función. En Deno ese import solo trae declaraciones de tipos; aquí no tiene
 * que aportar nada, solo existir para que el módulo se pueda resolver.
 */
export {};
